name: Production Preflight

on:
  push:
    branches: [main]
    paths:
      - 'build/**'
      - 'catalog/**'
      - 'production/**'
      - 'tracking/**'
      - '.github/workflows/production-preflight.yml'
  pull_request:
    paths:
      - 'build/**'
      - 'catalog/**'
      - 'production/**'
      - 'tracking/**'
      - '.github/workflows/production-preflight.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-production:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Compile production tools
        run: python -m compileall -q build/tools

      - name: Validate 30-book catalog and production manifests
        run: python build/tools/preflight_all.py

      - name: Validate Books 2-30 artwork job exports
        run: |
          for book in $(seq 2 30); do
            python build/tools/export_artwork_jobs.py --book "$book"
          done
